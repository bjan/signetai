[package]
name = "predictor"
version = "0.82.5"
edition = "2021"
license = "Apache-2.0"
description = "Signet predictive memory scorer sidecar"

[[bin]]
name = "signet-predictor"
path = "src/main.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rusqlite = { version = "0.32", features = ["bundled"] }
